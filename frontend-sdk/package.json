{
  "name": "frontend-sdk",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel index.html",
    "build": "npm run pretty && npm run lint && parcel build index.js --public-url '.'",
    "lint": "eslint index.js",
    "pretty": "prettier --config .prettierrc.json --write *",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.3.0",
    "parcel": "^2.0.1",
    "prettier": "^2.5.0"
  },
  "dependencies": {
    "qrcode": "^1.5.0"
  }
}
